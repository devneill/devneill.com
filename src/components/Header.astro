---
import Logo from "./Logo.astro";
import Nav from "./Nav.astro";

const { current = "" } = Astro.props;
---

<header class="w-full h-12 px-8 sm:px-12 mx-auto flex max-w-4xl">
    <Logo />
    <Nav current={current} />
</header>
